# cooking 标识

标识 = `.agents/cooking/<feature>/` 的目录名。

**命中**：用户消息里技能名之后的参数，第一段（按空白拆）等于某个已有子目录名。命中后只把这一段当标识，其余留给当前技能（如 `P2`、补充说明）。

未命中时不要按参数去 cooking 下新建目录。例外：`to-spec` 可从需求描述（或一段新的 kebab-case slug）生成并创建目录。需要列出已有标识时：只枚举子目录名，不要读目录正文。
