# 目标仓 `.agents/docs/ACCEPTANCE.md` 模板

方括号是占位。只写该仓库的事实。跳过项不得出现在「完成标准」里。

```markdown
# 验收

由 `acceptance` 技能生成。to-tasks 只把「完成标准」追加进各阶段；review 按本文件评，标明跳过的项不阻塞。

## 手段

- 类别：<PROJECT.md 类别，全栈加形态>
- 采用：<检索后的推荐，一句>
- 检索到的已有命令：<命令原文或「无」>
- 判定：<常规 | 纯原型 | 日常工作>

## 本机环境

| 命令 | 本机能否跑 | 标记 |
| --- | --- | --- |
| `<cmd>` | 能 / 不能 | 必跑 / 跳过 |

## 完成标准

只含必跑项。没有则写「无」。

- [ ] `<cmd>` 退出码 0

## 跳过

review 不得把下列项当阻塞。没有则写「无」。

- `<cmd>`：<原因，例如未安装 Playwright / 无设备 / 起不了服务>

## 运行

- 工作目录：仓库根
- 环境变量：<如 `ACCEPTANCE_BASE_URL`、鉴权变量；无则「无」>
- 鉴权：<用户所述方式；未采用 HTTP 脚本则「无」>
- 微服务入口 / compose：<仅 HTTP 脚本且适用时；否则「无」>
- 脚本路径：<`.agents/docs/acceptance/...` 或「无」>
```

纯原型 / 日常工作 / 无惯例不落脚手架：完成标准写「无」，跳过里写明原因，不要编造命令充数。
