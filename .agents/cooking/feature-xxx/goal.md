这个文件由 `/explore` 技能产生（可选：需求明确时可跳过 explore 直接 `/to-spec`）。会把用户和agent对话内容精炼润色展示出来, 最后给总结为一个需求目标(和对话内容分开来). 还包含决策树和确认状态(是否已确认). 这个文件必须要有固定的模板. 一次 explore 只对应一个 cooking 单位; 其它单位尚未完成也可以再开新的研讨, 冲突检查时基本上只读各未归档需求的 goal.md 即可.
