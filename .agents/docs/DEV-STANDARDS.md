这个文件由 `setup` 生成（规范变更时也由 setup 更新），包含了开发规范, 例如: 命名偏好(文件命名, 变量命名, 函数命名等等)、代码结构规范(目录规范, 比如后端接口代码的目录结构规范, 例如: controller、service、dao等等)、代码风格规范(缩进, 换行, 空格, 注释等等)、代码测试规范(单元测试, 集成测试, 性能测试等等)、接口设计和调用规范(响应格式、URL格式、 请求格式、错误处理规范等等)、数据库规范、日志规范、数据模型规范、版本规范等等.
