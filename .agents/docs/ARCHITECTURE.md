由 `setup` 技能生成（含后续架构更新），这个文件包含:

- 业务架构
- 技术架构. 包含: 技术栈

这个文件很重要, 也是需要一直择机更新的, 但是它的更新频率可能不会像CODE-MAP.md那样频繁, 例如添加、删除、更改了模块时, 可能不需要更新该文件, 一般是有大的架构调整, 会必须更新该文件, 防止出现业务架构和技术架构不一致的情况. 例如, 本身这个项目只有后端代码, 现在想要添加前端代码, 这个文件就必须更新. 还例如, 更换了技术栈、代码组织方式变化(拆成多包架构等等), 这个文件也必须更新.
